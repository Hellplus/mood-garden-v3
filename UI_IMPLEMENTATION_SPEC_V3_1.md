# Mood Garden V3.1 UI 实现规范

## 1. 文档目的

本文档用于约束 Codex 在 Mood Garden V3.1 UI 重设计阶段的前端实现行为。

它不是新的设计 Brief，也不是让 Codex 重新发明 UI 的开放式需求文档。它的作用是把已经确认的 UI 设计图、`DESIGN_SYSTEM_V3_1.md` 以及审查意见，转化为 Codex 可以执行的前端实现说明。

Codex 在实现时必须遵守以下原则：

```text
按已确认设计图实现
按 DESIGN_SYSTEM_V3_1.md 落地
不自行重新设计 UI
不新增业务功能
不改变 records localStorage 数据结构
不改变核心功能逻辑
不引入第三方 UI 库
```

本文件的目标是避免 UI 落地时出现以下问题：

```text
1. Codex 自由发挥颜色、卡片、按钮或图标；
2. 某个页面变好看，但其他页面风格不统一；
3. 移动端重新出现信息堆叠；
4. 设计图中的不可实现细节被强行照搬；
5. 视觉改版误伤已有业务功能；
6. 数据结构或 localStorage 逻辑被意外修改。
```

---

## 2. 已确认的 UI 参考图

本次 UI 落地必须以以下 9 张参考图为视觉依据。

### UI 参考图路径与验收标准

后续所有 UI 实现和验收必须使用项目内图片目录：

```text
pic/ui-reference/
```

具体文件如下：

```text
pic/ui-reference/01-final-visual-direction.png
pic/ui-reference/02-component-style-board.png
pic/ui-reference/03-empty-state-system.png
pic/ui-reference/04-emotion-icon-system.png
pic/ui-reference/05-mobile-record-page.png
pic/ui-reference/06-mobile-garden-page.png
pic/ui-reference/07-mobile-review-page.png
pic/ui-reference/08-mobile-data-page.png
pic/ui-reference/09-mobile-detail-modal.png
```

这些图片不是“可选参考”，而是后续 UI 实现和验收的视觉标准。Codex 必须按这些图进行对照实现和验收，不允许自行重新设计 UI。

用途对应关系：

```text
1. 01-final-visual-direction.png：用于整体视觉方向验收。
2. 02-component-style-board.png：是 DX.2 基础组件视觉验收主依据。
3. 03-empty-state-system.png：用于空状态验收。
4. 04-emotion-icon-system.png：用于情绪图标、底部导航图标、小花强度、星星收藏验收。
5. 05-mobile-record-page.png：用于移动端记录页验收。
6. 06-mobile-garden-page.png：用于移动端花园页验收。
7. 07-mobile-review-page.png：用于移动端回顾页验收。
8. 08-mobile-data-page.png：用于移动端数据页验收。
9. 09-mobile-detail-modal.png：用于详情弹窗、bottom sheet、删除确认弹窗验收。
```

### 01 最终视觉方向

用途：

```text
指导整体视觉方向、页面气质、桌面端和移动端的整体氛围。
```

主要参考内容：

```text
1. 现代治愈手账 App 风；
2. 米白背景、柔和浅绿、浅粉点缀；
3. 圆角卡片；
4. 低饱和色彩；
5. 花园感与手账感；
6. 电脑端完整网页应用布局；
7. 移动端 App 式组织。
```

Codex 不得偏离该图的整体风格，不得改成后台系统、极简冷淡风、赛博风或儿童插画风。

---

### 02 UI 组件样式板

用途：

```text
指导所有基础组件视觉，包括按钮、卡片、标签、Toast、弹窗、输入框、导航等。
```

主要参考内容：

```text
1. Button 系统；
2. Card 系统；
3. Chip 系统；
4. Toast；
5. Modal；
6. EmptyState；
7. BottomNav；
8. ThemeSwitcher；
9. 组件圆角、边框、阴影、间距、选中态。
```

DX.2 阶段必须主要参考此图。

---

### 03 空状态与插画统一

用途：

```text
指导所有空状态的文案、图标、插画和卡片呈现方式。
```

主要参考内容：

```text
1. 没有记录；
2. 今天还没有种花；
3. 筛选无结果；
4. 日历无记录；
5. 分析数据不足；
6. 没有标签；
7. 没有收藏；
8. 导入错误；
9. 备份提示。
```

Codex 可以用轻量 SVG、emoji + CSS、简单线性图标实现，不允许引入复杂插画资源。

---

### 04 统一情绪图标系统

用途：

```text
指导情绪图标、花朵图标、chip 小图标、日历小标记、底部导航图标和操作图标。
```

必须包含的情绪：

```text
开心
平静
焦虑
疲惫
兴奋
```

主要参考内容：

```text
1. 线性手绘风；
2. 圆润线条；
3. 低饱和颜色；
4. 小尺寸可读；
5. 情绪图标与文字同时出现；
6. 心情强度使用小花，不使用爱心；
7. 收藏使用星星。
```

---

### 05 移动端记录页

用途：

```text
指导移动端“记录”tab 的最终结构。
```

必须保留：

```text
1. 顶部问候；
2. 快速记录卡；
3. 情绪选择；
4. 心情输入框；
5. 种下一朵花按钮；
6. 更多记录选项折叠入口；
7. 今日状态；
8. 最近记录轻量预览；
9. 底部导航。
```

必须删除：

```text
花园 / 回顾 / 数据 三个入口卡
```

原因：这些入口已经存在于底部导航，重复展示会造成移动端首屏过载。

---

### 06 移动端花园页

用途：

```text
指导移动端“花园”tab 的结构和信息层级。
```

主要参考内容：

```text
1. 花卡片为主体；
2. 搜索入口清楚；
3. 筛选与排序默认折叠；
4. 标签云降噪；
5. 收藏、强度、标签展示克制；
6. 删除不暴露在卡片主视觉中；
7. 筛选无结果空状态清楚；
8. 底部导航不遮挡内容。
```

---

### 07 移动端回顾页

用途：

```text
指导移动端“回顾”tab 的结构和信息层级。
```

必须实现：

```text
今日 / 本周 / 本月 Tab 真实切换内容
```

禁止：

```text
把今日、本周、本月的全部内容一次性堆在同一个页面里
```

主要参考内容：

```text
1. 日历卡；
2. 今日洞察；
3. 本周洞察；
4. 本月洞察；
5. 情绪分布；
6. 趋势；
7. 标签 Top 5；
8. 收藏回顾；
9. 数据不足状态。
```

分析必须像生活回顾，不像医疗报告。

---

### 08 移动端数据页

用途：

```text
指导移动端“数据”tab 的结构、导入导出说明和本地数据安全说明。
```

必须包含：

```text
1. 备份与导出；
2. 恢复数据；
3. 本地保存说明；
4. PWA 添加到主屏幕说明；
5. 非医疗建议说明；
6. 危险操作区域；
7. Toast 示例；
8. 覆盖导入确认示例。
```

PWA 说明必须明确：

```text
添加到主屏幕只是方便打开。数据仍只保存在本设备，不会自动同步到其他设备。
```

---

### 09 移动端详情弹窗 / 操作弹层

用途：

```text
指导详情弹窗、编辑态、删除确认和移动端 bottom sheet 的视觉与结构。
```

必须实现状态区分：

```text
查看态：编辑 + 删除 + 关闭
编辑态：取消 + 保存
删除确认：独立 Dialog
```

移动端要求：

```text
1. 弹窗不被底部导航遮挡；
2. 内容过长时可滚动；
3. 底部操作 sticky；
4. 关闭按钮明显；
5. 删除确认清楚；
6. 不像后台表单。
```

---

## 3. 总体实现原则

### 3.1 按图实现，不自由发挥

Codex 必须按照以下优先级实现：

```text
UI 参考图
DESIGN_SYSTEM_V3_1.md
UI_IMPLEMENTATION_SPEC_V3_1.md
当前 React 项目结构
```

Codex 不允许自行重新设计：

```text
颜色
布局方向
图标风格
卡片风格
按钮层级
空状态风格
移动端信息结构
```

### 3.2 先还原设计系统，再优化细节

实现顺序必须是：

```text
1. Token
2. 基础组件
3. 核心页面
4. 移动端四页
5. 弹窗 / Toast / 空状态
6. 可访问性和回归
```

不允许一开始就全站大改。

### 3.3 功能逻辑不动

本次 UI 改版不得改变以下逻辑：

```text
记录新增
记录编辑
记录删除
收藏
标签
心情强度
筛选搜索排序
日历
分析
txt / JSON 导出
JSON 导入
主题
Toast
新手引导
移动端导航
PWA
```

### 3.4 移动端优先

移动端是本次重设计的重点。实现时必须优先保证：

```text
1. 记录页快速记录路径短；
2. 花园页卡片为主体；
3. 回顾页不堆叠分析；
4. 数据页说明清楚；
5. 底部导航不遮挡；
6. 无横向溢出。
```

### 3.5 桌面端保留完整网页布局

桌面端不能因为移动端 tab 逻辑被隐藏成单页面。

桌面端应保留：

```text
Hero
记录区
花园区
回顾区
数据区
```

### 3.6 不新增功能

禁止在 UI 实现中新增：

```text
后端
云同步
账号
AI 分析
复杂图表
社交分享
拖拽
长按
撤销删除
新的情绪类型
新的记录字段
```

### 3.7 不改数据结构

禁止修改：

```text
records localStorage 主结构
记录字段
导入导出 JSON 结构
localStorage key
```

---

## 4. 视觉还原优先级

### P0 必须还原

以下内容必须严格实现：

```text
1. 颜色系统；
2. 卡片圆角和边框；
3. 按钮层级；
4. 移动端四个 tab：记录 / 花园 / 回顾 / 数据；
5. 快速记录优先；
6. 花卡片视觉；
7. 空状态文案和图标；
8. 心情强度使用小花，不使用爱心；
9. 收藏使用星星；
10. PWA 文案说明不会自动同步；
11. 回顾页今日 / 本周 / 本月真实切换内容；
12. 记录页删除重复入口卡。
```

### P1 尽量还原

以下内容应尽量还原，但可根据实现成本简化：

```text
1. 线性手绘图标风格；
2. Toast 样式；
3. 弹窗样式；
4. 标签 chip；
5. 日历卡；
6. 分析卡；
7. 数据说明卡；
8. 移动端 bottom sheet 质感；
9. 新手引导卡片风格。
```

### P2 可简化实现

以下内容可以简化：

```text
1. 背景柔光；
2. 少量装饰点；
3. 轻微动画；
4. 空状态小插画；
5. 情绪图标细节；
6. 分析图表视觉；
7. 卡片轻微 hover。
```

简化原则：

```text
优先清楚可读
优先稳定
优先移动端性能
```

### P3 不实现

以下内容不实现：

```text
1. 大量背景插画；
2. 复杂水彩；
3. 浮动花瓣粒子；
4. 高拟真玻璃；
5. 复杂 3D；
6. 强动画；
7. 粒子动效；
8. 重度毛玻璃；
9. 高拟真插画资源；
10. 长按、拖拽等新交互。
```

---

## 5. 页面实现规范

## 5.1 桌面端首页

参考图：

```text
01 最终视觉方向
02 UI 组件样式板
```

### 桌面端整体关系

桌面端应保持完整网页应用布局，不使用移动端单 tab 隐藏结构。

推荐结构：

```text
Hero
↓
记录区：快速记录 + 今日状态 + 最近记录
↓
花园区：筛选 + 标签云 + 花卡片网格
↓
回顾区：日历 + 今日 / 本周 / 本月洞察
↓
数据区：导出导入 + 本地保存说明
```

### Hero

Hero 应包含：

```text
Mood Garden 情绪花园
温柔副标题
今日问候
轻量装饰
主题切换
```

Hero 不能过高，不能挤压快速记录区域。

### 快速记录

桌面端记录区可采用左右布局：

```text
左侧：快速记录卡
右侧：今日状态 + 最近记录
```

快速记录必须是视觉主任务。

### 花园区

花园区桌面端可展示完整筛选，但不能像后台筛选面板。

推荐：

```text
顶部搜索和筛选
标签云作为轻量贴纸
主体花卡片网格
```

### 回顾区

回顾区桌面端可显示更多内容，但仍需分层。

```text
日历
今日 / 本周 / 本月
分析卡片
标签和收藏回顾
```

### 数据区

数据区应分组：

```text
备份与导出
恢复数据
本地保存说明
PWA 说明
非医疗建议
危险操作
```

---

## 5.2 移动端记录页

参考图：

```text
05 移动端记录页
```

### 必须保留

```text
1. 顶部问候；
2. 快速记录卡；
3. 情绪选择；
4. 心情输入框；
5. 种下一朵花按钮；
6. 更多记录选项折叠入口；
7. 今日状态；
8. 最近记录轻量预览；
9. 底部导航。
```

### 必须删除

```text
花园 / 回顾 / 数据 三个入口卡
```

原因：这些入口已存在于底部导航中，重复出现会造成移动端首屏过载。

### 首屏优先级

首屏优先展示：

```text
问候
情绪选择
心情输入
种下一朵花
```

不应优先展示：

```text
完整标签云
完整分析图表
导入导出
完整花园列表
数据说明长文案
```

### 更多记录选项

默认折叠：

```text
心情强度
标签
自定义标签
```

折叠入口文案建议：

```text
添加更多细节
```

强度和标签即使未展开，也必须保持默认值可用：

```text
intensity 默认 3
tags 默认 []
```

---

## 5.3 移动端花园页

参考图：

```text
06 移动端花园页
```

### 页面目标

花园页的主体是查看和管理花卡片。

### 默认展示

```text
顶部标题：花园
说明文字
搜索入口
筛选与排序折叠入口
当前筛选摘要
花卡片列表
底部导航
```

### 筛选与排序

默认折叠，不直接铺满首屏。

折叠内容包括：

```text
情绪筛选
关键词搜索
排序
收藏筛选
强度筛选
标签云
重置筛选
```

### 标签云降噪

标签云不能占据过多高度。

建议：

```text
最多显示一到两行
超出使用横向滚动或“更多”
```

### 花卡片

花卡片必须包含：

```text
情绪图标
情绪名称
日期
心情摘要
标签 chip
强度小花
收藏星星
查看详情入口
```

### 删除操作

删除不暴露在花卡片主视觉中。

建议放在：

```text
详情弹窗
更多操作
删除确认弹窗
```

---

## 5.4 移动端回顾页

参考图：

```text
07 移动端回顾页
```

### 核心要求

```text
今日 / 本周 / 本月 Tab 必须真实切换内容
```

禁止：

```text
今日、本周、本月的所有卡片一次性堆在同一个页面
```

### 今日 Tab

显示：

```text
今日记录数量
今日情绪
今日强度
今日记录简短列表
今日小结
```

### 本周 Tab

显示：

```text
最近 7 天趋势
本周常见情绪
本周常用标签
本周总结
```

### 本月 Tab

显示：

```text
月历
本月记录数
本月情绪分布
最近 30 天趋势
月度总结
```

### 分析文案

必须生活化，不医疗化。

允许：

```text
这一周你记录了几朵花。
平静出现得比较多。
再多记录几天，趋势会更清楚。
```

禁止：

```text
你最近情绪异常。
你可能有心理问题。
你的焦虑水平较高。
```

---

## 5.5 移动端数据页

参考图：

```text
08 移动端数据页
```

### 分组结构

必须分为：

```text
1. 备份与导出
2. 恢复数据
3. 本地保存说明
4. 添加到主屏幕 / PWA 说明
5. 非医疗建议说明
6. 危险操作区域
```

### 备份与导出

包含：

```text
导出 txt 日记
导出 JSON 备份
```

说明：

```text
txt 适合阅读
JSON 适合备份和恢复
```

### 恢复数据

包含：

```text
选择 JSON 文件
合并导入
覆盖导入
```

说明：

```text
合并导入：把备份追加到当前花园
覆盖导入：用备份替换当前记录
```

覆盖导入前必须提示：

```text
建议先导出当前花园备份。
```

### 本地保存说明

必须包含：

```text
记录只保存在当前浏览器。
换设备不会自动同步。
清理浏览器数据可能导致记录丢失。
建议定期导出 JSON 备份。
```

### PWA 说明

必须包含：

```text
添加到主屏幕只是方便打开。
数据仍只保存在本设备，不会自动同步到其他设备。
```

### 危险操作

危险操作单独分组，使用浅红弱提示，不要和普通按钮混在一起。

---

## 5.6 详情弹窗

参考图：

```text
09 移动端详情弹窗 / 操作弹层
```

### 状态必须分开

详情弹窗必须有清楚状态机。

查看态：

```text
显示完整记录
按钮：编辑、删除、关闭
```

编辑态：

```text
显示可编辑字段
按钮：取消、保存
```

删除确认态：

```text
独立 Dialog
按钮：取消、确认删除
```

### 移动端 bottom sheet

移动端可使用 bottom sheet。

规则：

```text
最大高度不超过 90vh
内容区域可滚动
底部操作 sticky
不被底部导航遮挡
关闭按钮明显
```

### aria 要求

详情弹窗必须包含：

```text
role="dialog"
aria-modal="true"
aria-labelledby
```

Toast 不得替代弹窗确认。

---

## 6. 组件实现规范

以下组件必须参考 `02 UI 组件样式板` 实现。

### 6.1 Button

需要统一：

```text
主按钮
次按钮
文本按钮
图标按钮
危险按钮
禁用按钮
```

按钮必须有：

```text
hover
active
focus-visible
disabled
```

主按钮用于：

```text
种下一朵花
保存
导出 JSON
确认导入
```

危险按钮用于：

```text
删除
覆盖导入
清空
```

### 6.2 Card

基础卡片 class 应统一：

```text
圆角
边框
背景
阴影
padding
hover
```

应用于：

```text
快速记录卡
今日状态卡
最近记录卡
花卡片
日历卡
分析卡
数据卡
详情弹窗卡
```

### 6.3 Chip

Chip 类型：

```text
情绪 chip
标签 chip
筛选 chip
强度 chip
收藏 chip
```

必须具备：

```text
默认态
选中态
hover
focus-visible
```

情绪 chip 必须同时显示图标和文字。

### 6.4 FlowerCard

花卡片必须遵循：

```text
情绪图标突出
心情摘要清楚
标签克制
强度用小花
收藏用星星
删除不在主视觉中
```

### 6.5 CalendarCard

日历卡必须：

```text
轻量
不表格化
今日高亮
选中态清楚
有记录日期有小花点
```

### 6.6 AnalyticsCard

分析卡必须：

```text
低饱和
生活化
不过度医疗化
不强烈红绿对比
不堆太多图表
```

### 6.7 DataCard

数据卡必须：

```text
分组清楚
说明明确
危险操作独立
PWA 不误导同步
```

### 6.8 Toast

Toast 类型：

```text
success
error
info
```

Toast 必须：

```text
不遮挡移动端底部导航
不遮挡主按钮
支持 aria-live
自动消失
```

### 6.9 Modal

Modal 类型：

```text
详情弹窗
删除确认弹窗
新手引导弹窗
```

必须支持：

```text
Esc 关闭
关闭按钮
focus-visible
aria
移动端滚动
```

### 6.10 EmptyState

空状态组件必须支持：

```text
图标
标题
说明
可选按钮
不同场景文案
```

### 6.11 BottomNav

底部导航固定四项：

```text
记录
花园
回顾
数据
```

必须有：

```text
图标
文字
当前选中态
safe-area padding
触控区域 ≥44px
```

### 6.12 ThemeSwitcher

主题切换必须保持现有主题 key：

```text
light
dark
cozy
```

不新增主题 key。

---

## 7. 图标实现规范

参考图：

```text
04 统一情绪图标系统
```

### 7.1 实现方式

优先使用：

```text
简单 SVG
React 图标组件
CSS 变量控制颜色
```

不建议：

```text
外部图片
复杂插画
emoji 作为全部图标系统
多套图标风格混用
```

### 7.2 线宽和圆角

建议：

```text
线宽：1.5px - 2px
线帽：round
线连接：round
主尺寸：24px
chip 尺寸：16px
日历标记：10px - 12px
导航图标：24px - 28px
```

### 7.3 五种情绪图标

只保留：

```text
开心
平静
焦虑
疲惫
兴奋
```

建议：

```text
开心：小太阳 / 黄色小花
平静：叶子 / 绿色小花
焦虑：小云 / 雨滴
疲惫：月亮 / 低垂小花
兴奋：星星 / 粉色小花
```

### 7.4 底部导航图标

建议：

```text
记录：小芽 / 铅笔
花园：花朵
回顾：日历 / 手账
数据：文件盒 / 数据卡
```

### 7.5 操作图标

需要统一：

```text
编辑
删除
收藏
关闭
导出
导入
搜索
筛选
排序
主题
返回
```

### 7.6 小花强度图标

心情强度必须使用：

```text
1-5 朵小花
```

不使用爱心。

显示方式：

```text
小花数量 + 强度文字
```

例如：

```text
强度 3 · 🌼🌼🌼
```

### 7.7 星星收藏图标

收藏状态使用星星：

```text
未收藏：线性星星
已收藏：填充星星
```

不得使用爱心。

---

## 8. 空状态实现规范

参考图：

```text
03 空状态与插画统一
```

可以用：

```text
轻量 SVG
emoji + CSS
简单线性图标
```

不使用复杂插画。

### 8.1 没有记录

```text
标题：还没有记录噢
说明：从今天的一句话开始，把心情轻轻种下来。
按钮：去记录
```

### 8.2 今天还没有种花

```text
标题：今天还没有种花
说明：选一个此刻的心情，给今天留一朵小花。
按钮：种下一朵花
```

### 8.3 筛选无结果

```text
标题：没有找到符合条件的花朵
说明：试试调整筛选条件，看看更多心情记录。
按钮：清除筛选
```

### 8.4 日历无记录

```text
标题：这一天还安安静静
说明：等你记录时，它也会开出自己的小花。
```

### 8.5 分析数据不足

```text
标题：再多记录几天，趋势会更清楚
说明：持续记录后，这里会慢慢长出你的心情回顾。
```

### 8.6 没有标签

```text
标题：还没有贴上标签
说明：给心情加一个小标签，之后会更容易找到它们。
```

### 8.7 没有收藏

```text
标题：还没有收藏的瞬间
说明：遇到想保留的心情，可以轻轻点一下星星。
```

### 8.8 导入错误

```text
标题：这份文件没有被认出来
说明：请确认它是 Mood Garden 的 JSON 备份文件。
按钮：重新选择文件
```

### 8.9 备份提示

```text
标题：记得备份你的花园
说明：记录只保存在当前浏览器，定期导出 JSON 会更安心。
按钮：导出备份
```

---

## 9. 动画实现规范

### 9.1 必须做的动画

```text
1. 按钮 hover / active；
2. 卡片 hover 轻微上浮；
3. Toast 出入场；
4. Modal / bottom sheet 淡入或轻微滑入；
5. 主题切换颜色过渡；
6. focus-visible 明确反馈。
```

### 9.2 可选动画

```text
1. 情绪图标轻微呼吸；
2. Hero 小图标轻微漂浮；
3. 空状态图标轻微浮动；
4. 花卡片进入淡入；
5. 日历选中态轻微过渡。
```

### 9.3 禁止动画

```text
1. 粒子；
2. 大量花瓣飘落；
3. 强闪烁；
4. 复杂水彩流动；
5. 3D 翻转；
6. 大幅缩放；
7. 高频循环；
8. 影响阅读的动画。
```

### 9.4 prefers-reduced-motion

必须保留并完善：

```text
@media (prefers-reduced-motion: reduce)
```

减少动态时：

```text
取消位移
取消循环动画
取消强过渡
保留颜色反馈
```

---

## 10. Codex 分阶段实现计划

## DX.1：设计 token

### 可修改文件

```text
src/styles/base.css
src/styles/theme.css
src/styles/animations.css
src/styles/mobile.css 中基础变量相关内容
README.md
CHANGELOG.md
```

### 不可修改

```text
React 业务逻辑
records 数据结构
组件结构
页面布局
```

### 验收标准

```text
1. light / dark / cozy 主题正常；
2. CSS 变量建立；
3. focus-visible 清楚；
4. prefers-reduced-motion 存在；
5. lint/build 通过。
```

---

## DX.2：原子组件

### 可修改文件

```text
src/styles/components.css
src/styles/base.css
src/styles/mobile.css
相关组件 className
README.md
CHANGELOG.md
```

### 不可修改

```text
业务逻辑
记录结构
页面布局
移动端信息架构
```

### 验收标准

```text
1. Button 统一；
2. Card 统一；
3. Chip 统一；
4. Input / textarea / select 统一；
5. Toast / Modal 基础样式统一；
6. 空状态基础样式统一；
7. lint/build 通过。
```

---

## DX.3：核心页面

### 可修改文件

```text
HeroSection
RecordForm
TodayStatusCard
RecentRecords
GardenView
FlowerCard
相关 CSS
README.md
CHANGELOG.md
```

### 不可修改

```text
CRUD 逻辑
筛选逻辑
导入导出逻辑
数据结构
```

### 验收标准

```text
1. 记录页快速记录优先；
2. 花卡片符合设计；
3. 最近记录轻量；
4. 桌面首页结构清楚；
5. 移动端记录页无重复入口卡；
6. lint/build 通过。
```

---

## DX.4：移动端四页

### 可修改文件

```text
MobileBottomNav
GardenView
FilterPanel
TagCloud
CalendarView
AnalyticsDashboard
DataPanel
App.jsx 中移动端区域结构相关 class / UI 组织
相关 CSS
README.md
CHANGELOG.md
```

### 不可修改

```text
业务逻辑
records 数据结构
导入导出核心逻辑
分析计算逻辑
日历计算逻辑
```

### 验收标准

```text
1. 记录 / 花园 / 回顾 / 数据 四页清楚；
2. 花园页筛选折叠；
3. 回顾页 Tab 真实切换；
4. 数据页分组清楚；
5. PWA 文案说明不自动同步；
6. 底部导航不遮挡；
7. lint/build 通过。
```

---

## DX.5：弹窗、Toast、空状态

### 可修改文件

```text
FlowerDetailModal
Toast
OnboardingModal
EmptyState 相关组件
DataPanel 危险操作说明
相关 CSS
README.md
CHANGELOG.md
```

### 不可修改

```text
删除逻辑
保存逻辑
导入导出逻辑
数据结构
```

### 验收标准

```text
1. 详情弹窗查看态 / 编辑态分开；
2. 删除确认独立 Dialog；
3. 移动端 bottom sheet 可用；
4. Toast 统一；
5. 九种空状态统一；
6. aria 基础完整；
7. lint/build 通过。
```

---

## DX.6：可访问性和回归

### 可修改文件

```text
必要的 className
aria 属性
CSS focus
README.md
CHANGELOG.md
```

### 不可修改

```text
业务功能
数据结构
新增库
```

### 验收标准

```text
1. focus-visible 清楚；
2. 弹窗 aria 完整；
3. Toast aria 完整；
4. 移动端触控区域足够；
5. 不仅依赖颜色；
6. CRUD 正常；
7. 筛选正常；
8. 日历正常；
9. 分析正常；
10. 导入导出正常；
11. lint/build 通过。
```

---

## 11. Codex 禁止事项

Codex 在任何 DX 阶段都不得：

```text
1. 自动重新设计 UI；
2. 新增业务功能；
3. 修改 records localStorage 数据结构；
4. 修改核心 CRUD 逻辑；
5. 修改筛选、日历、分析、导入导出核心逻辑；
6. 引入第三方 UI 库；
7. 引入 Tailwind；
8. 引入 date-fns；
9. 引入 Framer Motion / GSAP；
10. 引入复杂图表库；
11. 增加后端；
12. 增加账号系统；
13. 增加云同步；
14. 增加 AI 分析；
15. 增加新的情绪类型；
16. 增加新的记录字段；
17. 照搬不可实现的视觉效果；
18. 让图片决定功能结构；
19. 一次性全站大改；
20. 用爱心表示心情强度；
21. 用颜色作为唯一信息；
22. 移除 focus-visible；
23. 让 PWA 文案暗示云同步；
24. 让移动端记录页出现重复入口卡；
25. 让回顾页 Tab 只是装饰。
```

---

## 12. 验收标准

最终 UI 落地后必须满足以下标准。

### 12.1 视觉一致性

```text
1. 与 01-09 参考图整体风格一致；
2. 颜色系统统一；
3. 卡片系统统一；
4. 按钮系统统一；
5. chip 系统统一；
6. 图标风格统一；
7. 空状态统一；
8. Toast 和弹窗统一。
```

### 12.2 移动端可用性

```text
1. 记录页清楚；
2. 花园页清楚；
3. 回顾页清楚；
4. 数据页清楚；
5. 底部导航不遮挡；
6. 无横向溢出；
7. 快速记录路径短；
8. 信息不堆叠。
```

### 12.3 功能保持不变

必须保持：

```text
1. 新增记录；
2. 编辑记录；
3. 删除记录；
4. 收藏；
5. 标签；
6. 强度；
7. 筛选；
8. 搜索；
9. 排序；
10. 日历；
11. 分析；
12. 导出 txt；
13. 导出 JSON；
14. 导入 JSON；
15. 主题；
16. Toast；
17. 新手引导；
18. 移动端导航；
19. PWA。
```

### 12.4 数据不丢

必须保证：

```text
1. records localStorage 主结构不变；
2. 已有记录不丢失；
3. 导入导出正常；
4. 刷新后数据保留；
5. 旧数据兼容不被破坏。
```

### 12.5 技术验收

必须通过：

```powershell
npm run lint
npm run build
```

### 12.6 Codex 实现验收

每个阶段完成后，Codex 必须说明：

```text
1. 修改了哪些文件；
2. 每个文件改了什么；
3. 是否新增业务功能；
4. 是否改变数据结构；
5. 是否修改业务逻辑；
6. 是否引入第三方库；
7. 如何测试；
8. lint/build 是否通过。
```

---

# 最终说明

`UI_IMPLEMENTATION_SPEC_V3_1.md` 是 Codex 实现 UI 的执行边界文件。

它的核心要求是：

```text
用户提供设计图
设计系统提供规则
实现规范限定范围
Codex 只负责实现
```

Codex 不负责重新设计 Mood Garden。
Codex 只能在已确认 UI 图和 `DESIGN_SYSTEM_V3_1.md` 的范围内，把视觉系统分阶段落地为 React + CSS。
