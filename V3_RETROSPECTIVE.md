# Mood Garden V3.0 React / Vite 重构复盘

这是一份个人学习复盘，用来记录 Mood Garden 从 V2.9 原生版本迁移到 V3.0 React / Vite 稳定版的过程。它不作为商业宣传，也不夸大当前能力；更多是整理一次前端重构、数据迁移和发布准备的经验。

## V3.0 的目标

V3.0 的目标不是重新做一个完全不同的产品，而是在保留 Mood Garden 核心体验的前提下，把项目从原生 HTML/CSS/JS 的单页实现，整理为更适合长期维护的 React / Vite 项目。

这一版主要希望完成几件事：

- 保留本地优先的心情记录方式；
- 保持浏览器 localStorage 数据链路；
- 将旧项目中集中在大脚本里的功能拆成组件、hooks 和 utils；
- 逐步迁移核心功能，而不是一次性复制旧代码；
- 让项目可以本地开发、构建、部署到 GitHub Pages；
- 为后续 V3.1 继续迭代留下清楚的结构。

## 为什么从 V2.9 原生版本重构到 React / Vite

V2.9 原生版本的好处是简单直接，HTML、CSS 和 JavaScript 都能快速看到结果。但随着功能增加，记录 CRUD、筛选、日历、分析、导入导出、主题、Toast、移动端导航和 PWA 等逻辑都挤在一起，继续维护会越来越困难。

迁移到 React / Vite 的主要原因是：

- React 更适合把页面拆成独立组件；
- hooks 可以把记录、筛选、日历、分析、主题、Toast 等状态逻辑分开；
- utils 可以沉淀可复用的数据处理函数；
- Vite 的开发和构建体验更清楚；
- 项目结构更适合阶段性迁移和回归测试；
- 后续如果要继续做更复杂的功能，代码边界会更清晰。

这次重构没有把 V2.9 的大段脚本直接搬进来，而是按功能重新拆分和实现，避免把旧问题原样带到新项目里。

## V3.0 的主要迁移阶段

### V3.0-A 方向评估

这一阶段主要确认迁移方向：保持 React + Vite + JavaScript，不引入 TypeScript、不引入第三方 UI 库、不引入状态管理库。目标是先把项目重构到更清楚的结构，而不是追求一次性做复杂架构。

### V3.0-B 项目骨架

这一阶段建立基础目录结构，包括 `components`、`hooks`、`utils`、`styles` 等目录。每个组件先以占位内容存在，保证项目可以正常启动，为后续逐步迁移提供边界。

### V3.0-C 静态 UI

这一阶段把基础骨架升级为接近 Mood Garden 气质的静态 UI。页面使用 mock 数据展示花园、记录、日历、分析和数据面板，但不接入真实业务逻辑。这样可以先确定视觉结构和组件职责。

### V3.0-D localStorage 与 CRUD

这一阶段迁移最核心的数据闭环：localStorage 读取和保存、记录 normalizer、新增、编辑、删除、收藏切换、详情弹窗、最近记录和花园列表真实渲染。

重点是保持旧数据兼容，避免损坏 localStorage 数据导致页面白屏。

### V3.0-E 筛选搜索排序

这一阶段加入真实浏览管理逻辑，包括情绪筛选、关键词搜索、最新/最早排序、标签云、点击标签筛选、收藏筛选、强度筛选、当前筛选状态提示和一键重置。

筛选只影响页面显示，不改变 localStorage 中的原始 records。

### V3.0-F 日历

这一阶段把静态日历迁移为基于真实 records 的日历视图。实现了本地日期 key、月份切换、回到今天、今天标记、有记录日期标记、点击日期查看当天记录。

日期处理使用原生 JavaScript，并尽量避免 UTC 时区导致的日期偏移。

### V3.0-G 数据分析

这一阶段把 mock 分析数据迁移为基于真实 records 的统计结果。包括总览、今日/本周/本月洞察、情绪分布、最近 7 天、最近 30 天趋势、强度分析、标签 Top 5、收藏回顾、连续记录天数和温柔回顾文案。

分析文案保持克制，只做记录回顾，不做心理诊断或医疗建议。

### V3.0-H 导入导出

这一阶段实现 TXT 日记导出、JSON 备份导出、JSON 导入、覆盖导入、合并导入和 id 冲突处理。导入数据统一经过 normalizer，兼容 V2.9 和 V3 当前结构。

导入导出只处理本地文件，不接后端，不做云同步。

### V3.0-I 主题、Toast、引导、移动端导航

这一阶段迁移体验层交互：light / dark / cozy 主题切换、Toast 提示、首次使用引导、移动端底部导航。主题和引导状态使用独立 localStorage key，不影响 records 主结构。

移动端通过 tab 切换记录、花园、分析、数据区域，桌面端仍保持完整布局。

### V3.0-J PWA

这一阶段加入基础 PWA 能力，包括 manifest、SVG 图标、手写 service worker 和生产环境注册逻辑。service worker 只缓存应用外壳和构建资源，不缓存用户记录数据。

PWA 能力保持简单，不引入 vite-plugin-pwa，不申请通知权限，不做推送通知。

### V3.0-K GitHub Pages 部署

这一阶段加入 GitHub Actions 部署配置，推送到 `main` 后构建 `dist` 并通过 GitHub Pages 发布。Vite base、manifest、service worker 路径都按 `/mood-garden-v3/` 处理。

部署方式不使用 `gh-pages` 包，也不从分支根目录直接发布源码。

### V3.0-L 最终回归与稳定版冻结

这一阶段进行最终回归验收，重点检查数据层、CRUD、筛选、日历、分析、导入导出、体验层交互、PWA、GitHub Pages 路径和文档。完成后将 V3.0 作为 React / Vite 稳定版冻结，为 V3.1 继续迭代做准备。

## React 组件拆分收获

这次重构最大的收获之一，是把页面从“一个页面里堆所有功能”拆成了更可读的组件。

例如：

- `RecordForm` 负责新增记录输入；
- `RecentRecords` 负责最近记录；
- `GardenView` 和 `FlowerCard` 负责花园列表；
- `FlowerDetailModal` 负责详情和编辑；
- `FilterPanel` 和 `TagCloud` 负责浏览管理；
- `CalendarView` 负责日历展示；
- `AnalyticsDashboard` 负责分析展示；
- `DataPanel` 负责导入导出；
- `ThemeSwitcher`、`Toast`、`OnboardingModal`、`MobileBottomNav` 负责体验层交互。

组件拆分后，后续修改某个区域时不需要在一个大文件里来回找逻辑，阅读负担明显降低。

## hooks 使用收获

hooks 的作用是把“会变化的状态和操作”从页面组件中抽出来。

这次比较有价值的 hooks 包括：

- `useRecords`：集中管理 records、CRUD、导入后的替换和合并；
- `useFilters`：集中管理筛选、搜索、排序状态；
- `useCalendar`：管理当前月份、选中日期和月历数据；
- `useAnalytics`：基于全部 records 输出分析数据；
- `useTheme`：管理主题和主题偏好；
- `useToast`：管理提示消息；
- `useOnboarding`：管理首次使用引导。

这样的拆分让 `App.jsx` 更像一个组织者：它负责连接数据、状态和组件，而不是塞满所有业务细节。

## utils 工具函数拆分收获

utils 的拆分让很多逻辑可以独立思考和复用。

这次比较关键的工具包括：

- `storage.js`：封装 localStorage 读取和保存；
- `records.js`：封装记录创建、normalize、筛选、排序、标签统计；
- `dates.js`：封装本地日期 key、月历数据和按日期分组；
- `analytics.js`：封装统计和回顾文案生成；
- `importExport.js`：封装 TXT / JSON 导出、JSON 导入解析和校验。

把这些逻辑放进 utils 后，组件只需要关心“展示什么”和“调用什么操作”，不必直接处理太多数据细节。

## 数据兼容和 localStorage 迁移收获

V3.0 数据迁移最重要的是控制风险。旧记录可能缺字段，localStorage 里也可能出现损坏 JSON。为此，V3.0 保留了 normalizer 和安全读取逻辑：

- 读取失败时返回空数组，避免页面白屏；
- 旧记录缺少字段时补齐默认值；
- 保留未知旧字段，避免无意丢数据；
- 导入 JSON 后也统一走 normalize；
- 筛选、日历、分析都基于 normalized records；
- records 主结构没有为了新功能频繁改变。

这次最大的经验是：数据层要比 UI 更保守。只要数据兼容稳定，后续 UI 和功能都还有调整空间。

## GitHub Actions / GitHub Pages 部署收获

V3.0-K 把部署流程明确下来：使用 GitHub Actions 构建 Vite 项目，并将 `dist` 发布到 GitHub Pages。

这里的关键点包括：

- Vite `base` 需要适配仓库路径 `/mood-garden-v3/`；
- manifest 的 `start_url` 和 `scope` 也要匹配 GitHub Pages 子路径；
- service worker 里的路径不能假设站点部署在域名根目录；
- GitHub Pages Source 应选择 GitHub Actions；
- 不使用 `gh-pages` 包，减少额外依赖。

这次也提醒我：部署不是最后随手补一下的事情，路径、缓存和构建输出都需要单独验收。

## 使用 Codex 的经验

这次使用 Codex 最大的帮助，是可以把重构拆成非常小的阶段，每一阶段都先分析、再实现、再验收。

比较有效的做法包括：

- 每个阶段明确“不做什么”，避免范围失控；
- 先做静态 UI，再接真实数据；
- 每次迁移后运行 lint 和 build；
- 对 localStorage、导入导出、PWA 缓存这些高风险点单独验收；
- 让 Codex 先说明方案，再执行代码修改；
- 发现问题时只修复明确问题，不顺手重构。

这种节奏比“一口气重写整个项目”更稳，也更适合学习。

## 遇到的风险和如何控制

这次主要风险有几类：

- 数据风险：通过 normalizer、安全读取和不改变 records 主结构来控制；
- 范围风险：每一阶段都明确不迁移的功能，避免把后续任务提前混进来；
- 旧代码风险：不复制 V2.9 大段脚本，而是按 React 结构重新实现；
- UI 风险：先做静态 UI，再逐步接入真实逻辑；
- 日期风险：使用本地日期 key，避免 UTC 偏移影响日历；
- 导入风险：导入 JSON 必须校验 records 数组，并处理 id 冲突；
- PWA 风险：service worker 只在生产环境注册，并保持缓存策略简单；
- 部署风险：统一检查 Vite base、manifest、service worker 和 GitHub Pages 路径；
- 移动端风险：底部导航只影响移动端，桌面端保持完整布局。

这些风险没有被完全消除，但都被拆成了可以检查、可以回归的点。

## V3.0 当前已知限制

V3.0 已经形成稳定版，但仍然有一些明确限制：

- 数据只保存在当前浏览器的 localStorage；
- 不提供账号系统、后端存储或云同步；
- PWA 只提供基础应用外壳缓存，不保证完整离线数据管理体验；
- 不提供推送通知；
- 不提供心理诊断、医学判断或医疗建议；
- 分析图表是轻量 UI，没有引入专业图表库；
- 导入导出仍以手动文件为主；
- 移动端体验可用，但仍有继续打磨空间；
- 目前没有自动化单元测试或端到端测试；
- 没有引入 React Router，多区域仍由页面内状态控制。

这些限制是有意保留的，目的是让 V3.0 先稳定下来，而不是在冻结前继续扩张范围。

## V3.1 后续方向建议

V3.1 可以在 V3.0 稳定结构上继续推进，但建议仍然分阶段进行。

可能的方向包括：

- 增加更系统的测试：优先覆盖 records normalizer、storage、import/export、dates 和 analytics；
- 优化移动端细节：底部导航间距、长文本、弹窗和表单体验；
- 改善无障碍体验：键盘操作、焦点管理、aria 文案和弹窗关闭体验；
- 增强导入导出：加入导入预览、冲突提示和更清楚的错误说明；
- 优化 PWA 更新提示：让用户知道有新版本可用，减少旧缓存困扰；
- 增加更清楚的数据备份提醒；
- 继续打磨分析文案，但保持不诊断、不夸大；
- 如果未来需要更复杂页面，再评估是否引入路由；
- 如果未来需要跨设备同步，再单独评估后端和隐私设计。

V3.1 不建议一开始就追求“大而全”。更适合先补测试、补体验、补可访问性，把 V3.0 已有的核心能力变得更稳。
