# Mood Garden 情绪花园 V3.1 设计系统

## 1. 设计目标

Mood Garden V3.1 的 UI 重设计目标，是在不新增业务功能、不改变数据结构、不重构产品逻辑的前提下，建立一套统一、温柔、清楚、可持续维护的视觉系统。

本次重设计主要解决以下问题：

1. 当前界面视觉偏简陋，缺少统一设计系统；
2. 图标、按钮、卡片、标签、空状态、弹窗和 Toast 风格不够统一；
3. 移动端部分页面存在信息密度偏高的问题；
4. 情绪记录产品需要更柔和、更有陪伴感的视觉表达；
5. 当前 UI 需要更适合长期使用，而不是只完成基本功能展示。

本次 UI 重设计的边界：

* 只做视觉系统、交互一致性、布局层级和文案呈现优化；
* 不新增业务功能；
* 不改变 records localStorage 数据结构；
* 不接后端；
* 不做云同步；
* 不做 AI 分析；
* 不做医疗或心理诊断表达；
* 不引入第三方 UI 库；
* 不引入 Tailwind；
* 不新增 date-fns；
* 不使用复杂动画库；
* 不改变现有核心功能链路。

---

## 2. 最终视觉方向

Mood Garden V3.1 的最终视觉方向定义为：

> **现代治愈手账 App 风**

整体气质应该是：

* 温柔但不幼稚；
* 成熟但不冷硬；
* 轻量但不单薄；
* 有花园感，但不过度装饰；
* 有手账感，但不变成贴纸堆叠；
* 有 App 感，但不变成后台管理系统。

视觉关键词：

```text
温柔
成熟
轻量
手账
花园
自然
陪伴
低压力
可长期使用
清楚可读
```

核心表达：

```text
把今天的心情，种成一朵花。
```

Mood Garden 不是心理分析工具，也不是效率管理工具。它更像一个安静的情绪手账，让用户每天用很低的成本记录当下，并在之后温柔地回看自己的心情花园。

---

## 3. 设计原则

### 3.1 移动端优先

移动端是 Mood Garden 的核心使用场景。用户最可能在手机上完成每日记录，因此移动端必须优先保证：

* 首屏能快速记录；
* 底部导航清楚；
* 四个区域任务明确；
* 不堆叠过多信息；
* 触控区域足够大；
* 底部导航不遮挡内容；
* 无横向滚动。

移动端四个主入口保持为：

```text
记录 / 花园 / 回顾 / 数据
```

### 3.2 快速记录优先

记录页的第一任务是让用户快速完成一次记录。

默认路径应是：

```text
选择情绪 → 写一句心情 → 种下一朵花
```

强度、标签、自定义标签属于更多记录选项，默认不应抢占首屏。

### 3.3 卡片柔和

界面主体由卡片构成，但卡片不应像后台面板。卡片应像温柔的手账纸片：

* 大圆角；
* 低对比边框；
* 轻阴影；
* 米白或半透明白底；
* 适度留白；
* 不使用厚重阴影；
* 不使用高对比粗边框。

### 3.4 图标统一

情绪图标、导航图标、操作图标、空状态插画必须属于同一视觉语言：

* 线性手绘风；
* 低饱和色；
* 圆润线条；
* 小尺寸可读；
* 不使用复杂 3D；
* 不使用拟真插画；
* 不使用多个不同风格混搭。

### 3.5 分析不医疗化

回顾和分析区域只做生活化回顾，不做心理诊断。

推荐表达：

```text
最近出现较多的是平静。
本周记录多了一些，趋势会慢慢更清楚。
这些标签最近常被你写下。
```

禁止表达：

```text
你的情绪异常。
你可能存在心理问题。
你最近状态不健康。
```

### 3.6 数据说明要安心

数据页应让用户清楚知道：

```text
记录只保存在当前浏览器。
换设备不会自动同步。
清理浏览器数据可能导致记录丢失。
建议定期导出 JSON 备份。
```

说明要清楚，但不要制造焦虑。

### 3.7 动画克制

动画只用于增强反馈，不用于装饰炫技。

允许：

* 卡片轻微上浮；
* 图标轻微呼吸；
* Toast 轻微滑入；
* 弹窗淡入；
* 主题切换轻微过渡。

禁止：

* 大量粒子；
* 强烈闪烁；
* 复杂水彩动态背景；
* 3D 翻转；
* 持续高频动效；
* 影响阅读的动效。

### 3.8 颜色不作为唯一信息

情绪、强度、收藏、危险操作不能只靠颜色表达。

必须同时使用：

* 文字；
* 图标；
* 状态样式；
* aria 或可访问性文本。

例如：

```text
开心 chip 不能只有黄色，还必须写“开心”。
收藏不能只变色，还要有星星图标和状态文本。
```

---

## 4. 业务与技术边界

本设计系统只指导 UI 与交互表现，不改变现有业务结构。

### 4.1 不允许新增的内容

* 不新增业务功能；
* 不新增情绪类型；
* 不新增记录字段；
* 不新增后端；
* 不新增账号系统；
* 不新增云同步；
* 不新增 AI 分析；
* 不新增社交分享体系；
* 不新增长按、拖拽等新交互；
* 不新增 Toast 撤销删除功能；
* 不新增复杂图表；
* 不新增复杂路由。

### 4.2 不允许改变的内容

* 不改变 records localStorage 主结构；
* 不改变现有 CRUD 逻辑；
* 不改变筛选、搜索、排序核心逻辑；
* 不改变日历计算逻辑；
* 不改变数据分析计算逻辑；
* 不改变导入导出数据格式；
* 不改变 PWA 基础逻辑。

### 4.3 技术实现约束

允许使用：

* CSS 变量；
* className；
* SVG 图标；
* CSS 渐变；
* 轻量 CSS 动画；
* React 组件现有结构上的小范围 class 调整。

不允许使用：

* 第三方 UI 库；
* Tailwind；
* date-fns；
* Framer Motion；
* GSAP；
* 复杂图表库；
* 复杂水彩背景；
* 重度玻璃拟态；
* 外部图片资源依赖；
* 大规模重构。

---

## 5. 颜色系统

### 5.1 浅色主题

浅色主题是默认主题，主视觉应保持米白、浅绿、浅粉的自然氛围。

建议 CSS 变量：

| 用途       | 变量名                     |                      建议值 | 说明            |
| -------- | ----------------------- | -----------------------: | ------------- |
| 页面背景     | `--color-bg`            |                `#FBF8F4` | 温暖米白          |
| 柔和背景     | `--color-bg-soft`       |                `#F6F1EA` | 区块背景          |
| 卡片背景     | `--color-card`          |                `#FFFFFF` | 主卡片           |
| 半透明卡片    | `--color-card-soft`     | `rgba(255,255,255,0.82)` | 可用于轻量卡片       |
| 主色       | `--color-primary`       |                `#6B8F6A` | 鼠尾草绿，加深以保证可读性 |
| 主色 hover | `--color-primary-hover` |                `#587A57` | 主按钮 hover     |
| 主色浅背景    | `--color-primary-soft`  |                `#E7F2E6` | 选中态、chip 背景   |
| 辅助粉      | `--color-accent-pink`   |                `#F7B8B7` | 温柔点缀          |
| 辅助黄      | `--color-accent-yellow` |                `#FFDFA6` | 开心、收藏点缀       |
| 辅助蓝      | `--color-accent-blue`   |                `#D8E7F5` | 信息提示          |
| 文字主色     | `--color-text`          |                `#2F332E` | 正文            |
| 文字次级     | `--color-text-muted`    |                `#6F756B` | 说明文字          |
| 文字弱化     | `--color-text-subtle`   |                `#9A9A90` | 辅助信息          |
| 边框       | `--color-border`        |                `#E7DED2` | 默认边框          |
| 分割线      | `--color-divider`       | `rgba(70, 60, 45, 0.12)` | 柔和分割          |
| 危险色      | `--color-danger`        |                `#D96B68` | 删除、覆盖导入       |
| 危险浅背景    | `--color-danger-soft`   |                `#FFF0EF` | 危险提示          |
| 成功色      | `--color-success`       |                `#6FA875` | 保存、导出成功       |
| 成功浅背景    | `--color-success-soft`  |                `#EAF5EC` | 成功 Toast      |
| 信息色      | `--color-info`          |                `#6F9EB8` | PWA、数据说明      |
| 信息浅背景    | `--color-info-soft`     |                `#EDF6FA` | 信息说明卡         |

### 5.2 深色主题

深色主题应是“夜晚花园”，不是黑色后台。

建议变量方向：

| 用途       | 变量名                     |                      建议值 |
| -------- | ----------------------- | -----------------------: |
| 页面背景     | `--color-bg`            |                `#171C18` |
| 柔和背景     | `--color-bg-soft`       |                `#20271F` |
| 卡片背景     | `--color-card`          |                `#273026` |
| 半透明卡片    | `--color-card-soft`     |    `rgba(39,48,38,0.86)` |
| 主色       | `--color-primary`       |                `#9BCB9E` |
| 主色 hover | `--color-primary-hover` |                `#B6DCB8` |
| 主色浅背景    | `--color-primary-soft`  | `rgba(155,203,158,0.16)` |
| 文字主色     | `--color-text`          |                `#F1EDE4` |
| 文字次级     | `--color-text-muted`    |                `#B8B0A4` |
| 边框       | `--color-border`        | `rgba(240,230,210,0.14)` |

深色主题禁止使用霓虹色、高对比纯黑纯白、赛博朋克渐变。

### 5.3 cozy / 治愈粉主题

cozy 主题是“晨光手账”，偏浅粉、米白、浅绿。

建议变量方向：

| 用途       | 变量名                     |       建议值 |
| -------- | ----------------------- | --------: |
| 页面背景     | `--color-bg`            | `#FFF7F5` |
| 柔和背景     | `--color-bg-soft`       | `#FFF0ED` |
| 卡片背景     | `--color-card`          | `#FFFFFF` |
| 主色       | `--color-primary`       | `#D98984` |
| 主色 hover | `--color-primary-hover` | `#C96F6A` |
| 主色浅背景    | `--color-primary-soft`  | `#FBE2DF` |
| 辅助绿      | `--color-accent-green`  | `#8FBF95` |
| 文字主色     | `--color-text`          | `#4A3632` |
| 文字次级     | `--color-text-muted`    | `#8F746F` |
| 边框       | `--color-border`        | `#EED7D2` |

cozy 主题不能变成糖果风。粉色只作为温柔主色，必须保留足够白空间和深色文字。

### 5.4 情绪色

情绪类型只保留当前五种：

```text
开心
平静
焦虑
疲惫
兴奋
```

建议变量：

| 情绪 | 变量名              |       建议色 | 图标方向       |
| -- | ---------------- | --------: | ---------- |
| 开心 | `--mood-happy`   | `#F2B84B` | 小太阳 / 黄色小花 |
| 平静 | `--mood-calm`    | `#8ABF9A` | 叶子 / 白绿小花  |
| 焦虑 | `--mood-anxious` | `#B8A6D9` | 小云 / 雨滴    |
| 疲惫 | `--mood-tired`   | `#9FB8D2` | 月亮 / 低垂花   |
| 兴奋 | `--mood-excited` | `#F28F98` | 星星 / 粉色小花  |

情绪色只能作为辅助识别，必须配合文字和图标。

### 5.5 对比度注意事项

* 主色绿色不要直接作为大段正文色；
* 链接和文本按钮的绿色需要使用加深版本；
* 浅粉、浅黄不能作为正文文字颜色；
* 禁用态不能只降低透明度到不可读；
* 移动端小字不得低于 13px；
* 图表颜色必须配合文字说明。

---

## 6. 字体系统

### 6.1 系统字体

不引入外部字体，使用系统字体栈。

建议字体方向：

```text
Inter
system-ui
-apple-system
BlinkMacSystemFont
Segoe UI
PingFang SC
Hiragino Sans GB
Microsoft YaHei
sans-serif
```

品牌标题可以通过字重、字距、颜色和图标搭配塑造气质，不加载特殊字体。

### 6.2 字号层级

| 类型       |     桌面端 |     移动端 | 建议       |
| -------- | ------: | ------: | -------- |
| 品牌标题     | 32-40px | 26-32px | 半粗或粗体    |
| 页面标题     | 28-32px | 22-26px | 清楚但不压迫   |
| 区块标题     | 18-22px | 17-20px | 可搭配小图标   |
| 卡片标题     | 16-18px | 16-17px | 半粗体      |
| 正文       | 15-16px | 14-16px | 不低于 14px |
| 说明文字     | 13-14px |    13px | 不低于 13px |
| 标签文字     | 12-13px | 12-13px | 小但可读     |
| 按钮文字     | 14-16px | 15-16px | 中等字重     |
| Toast 文案 |    14px | 13-14px | 简短明确     |

### 6.3 文案气质

推荐：

```text
今天想记录什么？
写下一句话，给今天留一朵小花。
你的花园还在等待第一朵花。
这一天还安安静静，等你种下一点心情。
```

避免：

```text
提交数据
执行导入
暂无数据
情绪异常
分析结果
```

---

## 7. 间距系统

### 7.1 页面宽度

桌面端：

```text
--layout-max-width: 1120px;
--layout-wide-max-width: 1280px;
--layout-padding-x: 32px;
```

移动端：

```text
--layout-padding-x-mobile: 16px;
```

### 7.2 页面与区块间距

| 类型      | 建议变量                      |  桌面端 |  移动端 |
| ------- | ------------------------- | ---: | ---: |
| 大区块间距   | `--space-section`         | 32px | 24px |
| 卡片组间距   | `--space-card-group`      | 20px | 16px |
| 卡片内边距   | `--space-card-padding`    | 20px | 16px |
| 小卡片内边距  | `--space-card-padding-sm` | 16px | 14px |
| 元素间距    | `--space-element`         | 12px | 10px |
| chip 间距 | `--space-chip-gap`        |  8px |  6px |

### 7.3 按钮高度

| 按钮    |     桌面端 |     移动端 |
| ----- | ------: | ------: |
| 主按钮   | 44-48px | 48-54px |
| 次按钮   | 38-42px | 44-48px |
| 图标按钮  | 36-40px | 40-44px |
| 底部导航项 |       - | 56-64px |

### 7.4 底部安全区

移动端底部必须预留：

```text
padding-bottom: calc(88px + env(safe-area-inset-bottom));
```

底部导航高度建议：

```text
64px - 72px
```

不允许底部导航遮挡表单按钮、弹窗按钮或数据操作按钮。

---

## 8. 圆角与阴影

### 8.1 圆角规则

| 组件    | 变量                 |   建议值 |
| ----- | ------------------ | ----: |
| 大卡片   | `--radius-card-lg` |  20px |
| 普通卡片  | `--radius-card`    |  16px |
| 小卡片   | `--radius-card-sm` |  14px |
| 按钮    | `--radius-button`  |  12px |
| 胶囊按钮  | `--radius-pill`    | 999px |
| chip  | `--radius-chip`    | 999px |
| 弹窗    | `--radius-modal`   |  24px |
| Toast | `--radius-toast`   |  16px |
| 输入框   | `--radius-input`   |  14px |

### 8.2 阴影规则

建议变量：

| 用途       | 变量名              | 说明                   |
| -------- | ---------------- | -------------------- |
| 轻阴影      | `--shadow-soft`  | 普通卡片                 |
| 中阴影      | `--shadow-card`  | 重点卡片                 |
| hover 阴影 | `--shadow-hover` | 卡片 hover             |
| 弹窗阴影     | `--shadow-modal` | Modal / bottom sheet |
| Toast 阴影 | `--shadow-toast` | Toast                |

阴影方向：

```text
柔和
低透明度
不厚重
不黑
不制造悬浮大面板感
```

禁止：

* 强黑色阴影；
* 多层重阴影；
* 大面积发光；
* 赛博风光晕。

---

## 9. 按钮系统

### 9.1 主按钮

用于最高优先级操作：

* 种下一朵花；
* 保存；
* 导出备份；
* 确认导入。

视觉规则：

```text
绿色主色
胶囊或 12px+ 圆角
白色文字
轻阴影
hover 微亮
active 微缩放
focus-visible 明显
```

主按钮文案应使用用户动作：

```text
种下一朵花
保存这朵花
导出我的花园
```

避免：

```text
提交
执行
确认
```

### 9.2 次按钮

用于低一级操作：

* 查看详情；
* 查看更多；
* 取消；
* 导出文本；
* 选择文件。

视觉规则：

```text
白色或浅色背景
绿色或深色文字
浅边框
无重阴影
```

### 9.3 文本按钮

用于轻量操作：

* 清空筛选；
* 收起；
* 查看全部；
* 返回。

视觉规则：

```text
透明背景
主色深色文字
hover 下划线或浅背景
不要像主按钮
```

### 9.4 图标按钮

用于：

* 收藏；
* 编辑；
* 删除；
* 关闭；
* 主题切换；
* 更多菜单。

规则：

```text
圆形或小胶囊
最小触控面积 40px
图标线宽统一
hover 有浅背景
focus-visible 保留
```

### 9.5 危险按钮

用于：

* 删除记录；
* 覆盖导入；
* 清空所有记录。

规则：

```text
默认使用浅红背景 + 红色文字 + 红色边框
最终确认按钮可使用红色实心
危险操作必须配合确认弹窗
不能和普通主按钮混淆
```

### 9.6 禁用态

禁用态必须仍可读：

```text
降低饱和度
文字不低于可读对比
cursor: not-allowed
不能只设置 opacity: 0.2
```

### 9.7 交互状态

所有可点击元素必须有：

```text
hover
active
focus-visible
disabled
```

focus-visible 不能移除。

---

## 10. 卡片系统

### 10.1 快速记录卡

快速记录卡是记录页核心。

必须突出：

```text
情绪选择
心情输入
种下一朵花按钮
```

更多记录选项默认折叠。

视觉规则：

* 大圆角；
* 白色或米白卡片；
* 输入框清楚；
* 主按钮显眼；
* 情绪 chip 容易点击；
* 移动端首屏不被次级信息抢占。

### 10.2 今日状态卡

今日状态卡用于展示今日记录状态。

内容：

* 今日是否已记录；
* 今日心情；
* 今日记录数量；
* 当前情绪花；
* 简短鼓励文案。

视觉：

* 轻量花盆或花朵图标；
* 不过度大；
* 移动端放在快速记录卡之后；
* 不抢主按钮。

### 10.3 最近记录卡

最近记录应轻量展示。

规则：

* 显示 1-3 条；
* 每条最多两行；
* 保留情绪文字；
* 保留时间；
* 可进入详情；
* 不显示过多操作按钮。

### 10.4 花卡片

花卡片是 Mood Garden 的核心视觉单元。

内容：

* 情绪图标；
* 情绪名称；
* 日期；
* 心情摘要；
* 标签；
* 强度小花；
* 收藏星星；
* 查看详情入口。

规则：

* 删除按钮不应暴露在卡片主视觉里；
* 移动端单列；
* 桌面端网格；
* hover 轻微上浮；
* 保持信息克制。

### 10.5 日历卡

规则：

* 像手账月历；
* 今日高亮；
* 选中日期明确；
* 有记录日期显示小花点；
* 不使用重表格边框；
* 日历小标记必须可读。

### 10.6 分析卡

分析卡必须生活化。

规则：

* 不像医疗报告；
* 图表简洁；
* 使用低饱和色；
* 结合自然语言摘要；
* 数据不足时显示温柔提示。

### 10.7 数据说明卡

数据卡必须让用户安心。

内容：

* 本地保存；
* JSON 备份；
* 合并导入；
* 覆盖导入；
* PWA 添加到主屏幕；
* 非医疗建议说明。

规则：

* 清楚分组；
* 危险操作独立视觉；
* 文案明确但不恐吓。

### 10.8 详情弹窗卡

详情弹窗必须区分：

```text
查看态
编辑态
删除确认态
```

查看态：

```text
查看完整记录
编辑
删除
关闭
```

编辑态：

```text
可编辑字段
取消
保存
```

删除确认：

```text
独立 Dialog
说明删除不可恢复
取消
确认删除
```

---

## 11. 标签和筛选系统

### 11.1 情绪 chip

情绪 chip 用于：

* 记录表单；
* 筛选；
* 花卡片；
* 分析标识。

必须包含：

```text
图标 + 情绪文字
```

不能只用颜色。

情绪只保留：

```text
开心 / 平静 / 焦虑 / 疲惫 / 兴奋
```

### 11.2 标签 chip

标签 chip 像轻量贴纸。

规则：

* 圆角胶囊；
* 浅色背景；
* 文字清楚；
* 最多显示 3 个，超出显示 `+N`；
* 移动端不换行过多。

### 11.3 筛选 chip

筛选 chip 用于表示当前筛选条件。

规则：

* 选中态明显；
* 不选中态轻；
* 重置筛选清楚；
* 移动端放入折叠区。

### 11.4 收藏状态

收藏使用星星。

状态：

```text
未收藏：线性星星
已收藏：填充星星 / 浅黄色
```

收藏不能使用爱心。

### 11.5 强度选择

心情强度必须改为：

```text
1-5 朵小花
```

不再使用爱心。

规则：

* 当前强度对应小花数量；
* 未选中小花使用浅描边；
* 选中小花使用情绪色或主色；
* 保留数字或文字辅助，例如“强度 3”；
* 不能只靠小花颜色表示。

### 11.6 重置筛选

重置筛选为文本按钮或次按钮，不使用危险按钮。

文案建议：

```text
清除筛选
重置筛选
看看全部花朵
```

### 11.7 移动端筛选折叠

移动端花园页默认只展示：

```text
搜索
筛选与排序入口
当前筛选摘要
花卡片列表
```

完整筛选项放入：

```text
筛选与排序折叠区
```

标签云默认不展开全部，最多展示一行或两行。

---

## 12. 图标系统

### 12.1 总体风格

图标系统采用：

```text
线性手绘风
圆润线条
低饱和色
统一线宽
小尺寸可读
```

建议：

```text
线宽：1.5px - 2px
圆角：round
端点：round
主尺寸：24px
chip 尺寸：16px
日历标记：10px - 12px
导航图标：24px - 28px
```

### 12.2 五种情绪图标

| 情绪 | 主图标        | 花朵变体 | 动画建议          |
| -- | ---------- | ---- | ------------- |
| 开心 | 小太阳 / 微笑小花 | 黄色小花 | 轻微上扬、微笑闪动     |
| 平静 | 叶子 / 白绿小花  | 绿色小花 | 慢速呼吸、叶子轻摆     |
| 焦虑 | 小云 / 雨滴    | 紫蓝小花 | 云朵轻微漂浮，雨滴缓慢落下 |
| 疲惫 | 月亮 / 低垂花   | 蓝灰小花 | 慢速下垂、轻微摇晃     |
| 兴奋 | 星星 / 粉色小花  | 粉色小花 | 轻微闪烁、上跳一次     |

动画必须克制，不持续高频。

### 12.3 chip 小图标

chip 图标只做辅助识别。

规则：

* 小尺寸必须可读；
* 不要细节过多；
* 颜色低饱和；
* 必须配合文字。

### 12.4 日历小标记

日历小标记建议使用：

```text
小花点
小圆点
迷你情绪图标
```

同一天多条记录时最多显示 3 个点，其余用数量或省略表示。

### 12.5 底部导航图标

四个底部导航图标：

| 导航 | 图标建议       |
| -- | ---------- |
| 记录 | 铅笔 / 小芽    |
| 花园 | 花朵         |
| 回顾 | 打开的手账 / 日历 |
| 数据 | 柱状图 / 文件盒  |

图标必须和文字一起出现。

### 12.6 操作图标

操作图标包括：

```text
编辑
删除
收藏
关闭
导出
导入
搜索
筛选
排序
设置
返回
```

删除图标使用红色系，但默认不要过于刺眼。

### 12.7 SVG 实现建议

优先使用内联 SVG 或组件化 SVG。

不建议：

* 使用外部图片作为核心图标；
* 使用过大插画作为功能图标；
* 使用 emoji 替代所有图标；
* 多套图标风格混用。

---

## 13. 空状态系统

空状态必须有独立场景，不使用统一“暂无数据”。

### 13.1 没有记录

主文案：

```text
还没有记录噢
```

辅助文案：

```text
从今天的一句话开始，把心情轻轻种下来。
```

按钮：

```text
去记录
```

插画：

```text
小花苞 / 便签 / 小芽
```

### 13.2 今天还没有种花

主文案：

```text
今天还没有种花
```

辅助文案：

```text
选一个此刻的心情，给今天留一朵小花。
```

按钮：

```text
种下一朵花
```

### 13.3 筛选无结果

主文案：

```text
没有找到符合条件的花朵
```

辅助文案：

```text
试试调整筛选条件，看看更多心情记录。
```

按钮：

```text
清除筛选
```

### 13.4 日历无记录

主文案：

```text
这一天还安安静静
```

辅助文案：

```text
等你记录时，它也会开出自己的小花。
```

### 13.5 分析数据不足

主文案：

```text
再多记录几天，趋势会更清楚
```

辅助文案：

```text
持续记录后，这里会慢慢长出你的心情回顾。
```

### 13.6 没有标签

主文案：

```text
还没有贴上标签
```

辅助文案：

```text
给心情加一个小标签，之后会更容易找到它们。
```

### 13.7 没有收藏

主文案：

```text
还没有收藏的瞬间
```

辅助文案：

```text
遇到想保留的心情，可以轻轻点一下星星。
```

### 13.8 导入错误

主文案：

```text
这份文件没有被认出来
```

辅助文案：

```text
请确认它是 Mood Garden 的 JSON 备份文件。
```

按钮：

```text
重新选择文件
```

### 13.9 备份提示

主文案：

```text
记得备份你的花园
```

辅助文案：

```text
记录只保存在当前浏览器，定期导出 JSON 会更安心。
```

按钮：

```text
导出备份
```

---

## 14. Toast 与弹窗系统

### 14.1 Toast 位置

桌面端：

```text
右上角或顶部右侧
```

移动端：

```text
顶部下方
不遮挡底部导航
不遮挡主按钮
```

### 14.2 Toast 类型

| 类型      | 用途          | 视觉          |
| ------- | ----------- | ----------- |
| success | 保存、导出、导入成功  | 浅绿背景 + 成功图标 |
| error   | 导入错误、保存失败   | 浅红背景 + 错误图标 |
| info    | 数据说明、PWA 提示 | 浅蓝背景 + 信息图标 |

### 14.3 Toast aria

Toast 必须使用：

```text
role="status"
aria-live="polite"
```

错误 Toast 可以使用：

```text
role="alert"
```

### 14.4 详情弹窗

详情弹窗必须支持：

* 查看态；
* 编辑态；
* 关闭；
* 移动端滚动；
* 底部 sticky 操作区；
* role="dialog"；
* aria-modal="true"；
* aria-labelledby；
* 打开后焦点进入弹窗；
* 关闭后焦点回到触发按钮。

### 14.5 删除确认弹窗

删除确认必须独立，不与详情查看态混在一起。

内容：

```text
确认删除这条记录？
删除后无法恢复，但不会影响其他记录。
```

按钮：

```text
取消
确认删除
```

### 14.6 新手引导弹窗

新手引导只说明核心动作：

```text
选择一个情绪
写下一句话
种下一朵花
记录保存在当前浏览器
可以导出 JSON 备份
```

不讲复杂技术词。

### 14.7 移动端 bottom sheet

移动端详情弹窗可以使用 bottom sheet。

规则：

* 顶部有拖拽视觉把手，但不新增真实拖拽交互；
* 高度不超过视口 90%；
* 内容区域可滚动；
* 底部按钮 sticky；
* 不被底部导航遮挡。

### 14.8 查看态 / 编辑态状态机

详情弹窗状态必须清楚：

```text
查看态：
显示完整记录
按钮：编辑、删除、关闭

编辑态：
显示可编辑字段
按钮：取消、保存

删除确认：
独立确认弹窗
按钮：取消、确认删除
```

---

## 15. 移动端规范

### 15.1 记录页

记录页只保留：

```text
问候
快速记录卡
今日状态
最近记录轻量预览
底部导航
```

必须删除：

```text
花园 / 回顾 / 数据 三个入口卡
```

原因：这三个入口已经存在于底部导航中，重复展示会造成移动端首屏过载。

记录页首屏优先级：

```text
1. 今日问候
2. 情绪选择
3. 心情输入
4. 种下一朵花
5. 更多记录选项折叠入口
```

### 15.2 花园页

花园页主体是花卡片列表。

默认展示：

```text
标题
搜索
筛选与排序折叠入口
当前筛选摘要
花卡片列表
```

默认不展开：

```text
完整筛选
标签云
强度筛选
收藏筛选
排序细项
```

筛选与排序应折叠展示。

### 15.3 回顾页

回顾页包含日历和分析，但必须避免堆叠。

顶部 Tab：

```text
今日 / 本周 / 本月
```

必须真实切换内容。

今日 Tab：

```text
今日洞察
今日记录
今日小结
```

本周 Tab：

```text
本周趋势
本周常用标签
本周总结
```

本月 Tab：

```text
月历
月度情绪分布
月度趋势
月度总结
```

禁止把今日、本周、本月所有内容一次性堆在一个页面中。

### 15.4 数据页

数据页分组：

```text
备份与导出
恢复数据
本地保存说明
添加到主屏幕
非医疗建议说明
危险操作区域
```

PWA 说明必须写：

```text
添加到主屏幕只是方便打开。数据仍只保存在本设备，不会自动同步到其他设备。
```

### 15.5 详情弹窗

移动端详情弹窗：

* 使用 bottom sheet 或居中大圆角弹窗；
* 内容可滚动；
* 底部操作 sticky；
* 删除使用独立确认；
* 不被底部导航遮挡；
* 关闭按钮明显。

### 15.6 底部导航

底部导航固定四项：

```text
记录
花园
回顾
数据
```

规则：

* 图标 + 文字；
* 当前项高亮；
* 触控区域 ≥44px；
* 使用 safe area padding；
* 不新增更多入口；
* 不使用二级菜单。

### 15.7 默认折叠内容

移动端默认折叠：

* 更多记录选项；
* 标签输入；
* 强度选择；
* 花园筛选；
* 标签云；
* 数据说明长文案；
* PWA 说明；
* 非医疗建议说明。

### 15.8 不能出现在首屏的内容

记录页首屏不能出现：

* 大量分析图表；
* 数据导入导出；
* 完整花园列表；
* 完整标签云；
* 过长说明；
* 重复入口卡；
* 危险操作。

---

## 16. 电脑端规范

### 16.1 最大内容宽度

桌面端主体建议：

```text
max-width: 1120px - 1280px
居中布局
左右留出柔和背景
```

不要铺满大屏幕。

### 16.2 Hero

Hero 包含：

* 品牌标题；
* 今日问候；
* 副标题；
* 轻量装饰；
* 主题切换。

Hero 不宜过高，避免抢掉快速记录区域。

### 16.3 记录区

桌面端可使用左右布局：

```text
左侧：快速记录卡
右侧：今日状态 + 最近记录
```

快速记录仍为视觉主任务。

### 16.4 花园区

桌面端花园区：

```text
顶部：搜索 + 筛选
侧边或顶部：标签云
主体：花卡片网格
```

筛选区可完整展示，但不能像后台筛选面板。

### 16.5 回顾区

桌面端回顾区可以比移动端展示更多内容，但仍需分层：

```text
今日 / 本周 / 本月
日历
分析卡片
趋势
标签
收藏回顾
```

分析文案不医疗化。

### 16.6 数据区

桌面端数据区：

```text
备份导出
恢复数据
本地保存说明
PWA 说明
非医疗建议
危险操作
```

分组清楚，不做后台设置页。

### 16.7 桌面端不要被移动端逻辑破坏

移动端 tab 逻辑只影响小屏幕。桌面端应保持完整页面布局，不应因为移动端 active section 隐藏内容。

---

## 17. 动画系统

### 17.1 页面进入

允许轻微：

```text
opacity 0 → 1
translateY 6px → 0
duration 180-260ms
```

不使用大幅位移。

### 17.2 卡片 hover

允许：

```text
轻微上浮
阴影增强
边框略微加深
```

不使用旋转、翻转、强缩放。

### 17.3 图标轻微动画

允许：

* 平静叶子慢速轻摆；
* 开心小花轻微上扬；
* 焦虑云朵轻微漂浮；
* 疲惫月亮慢速呼吸；
* 兴奋星星轻微闪一下。

不允许持续高频动画。

### 17.4 Toast

Toast 动画：

```text
轻微向下 / 向上滑入
淡入
自动消失
```

### 17.5 弹窗

弹窗动画：

```text
overlay 淡入
卡片轻微 scale 0.98 → 1
bottom sheet 从底部轻微上滑
```

### 17.6 主题切换

主题切换：

```text
background-color
color
border-color
box-shadow
```

使用 150-250ms 过渡。

### 17.7 prefers-reduced-motion

必须支持：

```text
@media (prefers-reduced-motion: reduce)
```

在减少动态效果时：

* 取消浮动动画；
* 取消 hover 位移；
* 保留颜色变化；
* 保留基础可用性。

### 17.8 禁止动画

禁止：

* 粒子雨；
* 大量花瓣飘落；
* 强闪烁；
* 复杂水彩流动；
* 3D 翻转；
* 旋转眩晕效果；
* 长时间循环装饰动画。

---

## 18. 可访问性规范

### 18.1 对比度

* 正文文字与背景必须具备足够对比；
* 主色绿色不能作为浅背景上的小字号正文；
* 浅粉、浅黄不能作为正文色；
* 图表必须有文字说明。

### 18.2 focus-visible

所有交互元素必须保留 focus-visible：

* 按钮；
* 输入框；
* chip；
* 底部导航；
* 弹窗关闭按钮；
* 文件选择；
* 导入导出按钮。

### 18.3 表单 label

RecordForm 输入项必须有可访问 label。

placeholder 不能替代 label。

### 18.4 弹窗焦点

弹窗打开时：

* 焦点进入弹窗；
* 背景内容不应被键盘焦点访问；
* Esc 可关闭；
* 关闭后焦点回到触发按钮。

### 18.5 Toast aria

Toast 使用：

```text
role="status"
aria-live="polite"
```

错误 Toast 使用：

```text
role="alert"
```

### 18.6 触控区域

移动端可点击元素触控区域不小于：

```text
44px × 44px
```

### 18.7 不仅依赖颜色

必须同时使用：

* 文本；
* 图标；
* 状态；
* aria。

例如：

```text
收藏：星星 + 已收藏文字
强度：小花数量 + 强度数字
情绪：图标 + 情绪名称
```

### 18.8 减少动态效果

必须支持 prefers-reduced-motion。

---

## 19. Codex 实施优先级

### 阶段 1：设计 Token 与基础样式

目标：

```text
建立颜色、字体、间距、圆角、阴影、基础动画变量。
```

范围：

* CSS variables；
* theme.css；
* base.css；
* 通用 focus-visible；
* prefers-reduced-motion；
* 不改业务逻辑。

### 阶段 2：原子组件和基础 UI

目标：

```text
统一按钮、chip、卡片、图标、输入框、Toast 基础样式。
```

范围：

* Button class；
* chip class；
* card class；
* icon button；
* empty state 基础 class；
* modal 基础 class。

### 阶段 3：核心页面重设计

目标：

```text
重做记录页和首页核心区域。
```

范围：

* Hero；
* RecordForm；
* TodayStatusCard；
* RecentRecords；
* 快速记录优先；
* 移动端记录页删除重复入口卡。

### 阶段 4：移动端页面重设计

目标：

```text
重做移动端花园、回顾、数据页的信息层级。
```

范围：

* 花园筛选折叠；
* 标签云降噪；
* 回顾 Tab 真实切换；
* 数据页分组；
* PWA 不自动同步文案。

### 阶段 5：弹窗、Toast、空状态

目标：

```text
统一反馈和状态体验。
```

范围：

* 详情弹窗查看态 / 编辑态；
* 删除确认；
* Toast；
* 九种空状态；
* 移动端 bottom sheet；
* aria。

### 阶段 6：可访问性和回归测试

目标：

```text
确保视觉改版不破坏功能和可访问性。
```

范围：

* lint；
* build；
* 移动端测试；
* 主题测试；
* 键盘测试；
* 导入导出测试；
* PWA 检查。

---

## 20. 禁止事项

本次 V3.1-DX UI 落地禁止：

1. 新增业务功能；
2. 改变 records localStorage 主结构；
3. 新增后端；
4. 新增账号系统；
5. 新增云同步；
6. 新增 AI 分析；
7. 新增医疗或心理诊断表达；
8. 新增 date-fns；
9. 使用第三方 UI 库；
10. 使用 Tailwind；
11. 使用 Framer Motion；
12. 使用 GSAP；
13. 使用复杂图表库；
14. 实现浮动花瓣粒子；
15. 照搬高拟真水彩背景；
16. 做重度玻璃拟态；
17. 做复杂 3D；
18. 做强闪烁动画；
19. 做长按、拖拽等新交互；
20. 新增 Toast 撤销删除功能；
21. 删除现有核心功能；
22. 让移动端首屏出现大量统计；
23. 让记录页出现重复的花园 / 回顾 / 数据入口卡；
24. 用爱心表示心情强度；
25. 让回顾页 Tab 只做装饰；
26. 让 PWA 文案暗示支持云同步；
27. 只用颜色表达情绪或状态；
28. 移除 focus-visible；
29. 让底部导航遮挡主要操作；
30. 破坏桌面端完整布局。

---

## 最终落地原则

Mood Garden V3.1 的 UI 重设计不是“换皮肤”，而是建立一套能长期维护的视觉系统。

最终实现时应遵循：

```text
先 token
再组件
再页面
再移动端
再反馈状态
最后回归测试
```

任何视觉优化都不能优先于：

```text
记录顺畅
数据安全
移动端清楚
可访问性
功能稳定
```
